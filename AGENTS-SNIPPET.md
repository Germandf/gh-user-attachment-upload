## PR Evidence Screenshots
- If the user explicitly asks to upload screenshots through GitHub `user-attachments`, use `gh-upload-image owner/repo path\image.png`; it prints Markdown links. Requires local `GH_USER_SESSION`; never ask the user to paste that cookie in chat and never echo it in logs.
